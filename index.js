const inquirer = require("inquirer");
const fs = require("fs");

const generateReadMe = (answers) =>
``;

inquirer 
    .prompt([
        {
            type: "",
            name: "",
            message: "",
        }
    ])
